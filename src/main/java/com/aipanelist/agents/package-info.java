/**
 * AI agent implementations.
 * 
 * This package contains the base AI agent class and specialized agent implementations
 * for different analysis perspectives (lead analyst, methodology reviewer, etc.).
 * Each agent analyzes documents independently and generates specialized reports.
 */
package com.aipanelist.agents;
